---
####################################################
#          ModuleBuilder Configuration             #
####################################################
CopyPaths:
  - en-US
  - Modules
Encoding: UTF8
VersionedOutputDirectory: true

# '.psm1' Prefix and Suffixes
Prefix: prefix.ps1
#Suffix: suffix.ps1

####################################################
#     Dependent Modules Configuration (Sampler)    #
####################################################
NestedModule:

####################################################
#            Pipeline Configuration                #
####################################################
BuildWorkflow:
  '.':
    - build
    - test

  build:
    - Clean
    - Build_Module_ModuleBuilder
    - Copy_LCM_Rules

  pack:
    - build
    - package_module_nupkg

  test:
    - Pester_Tests_Stop_On_Fail
    - Pester_if_Code_Coverage_Under_Threshold

  publish:
    # Ordered so the reversible step runs first: if the GitHub release fails, nothing has
    # been pushed to the PowerShell Gallery, where a published version cannot be replaced.
    - Publish_release_to_GitHub
    - publish_module_to_gallery
    # Publish_GitHub_Wiki_Content is provided by DscResource.DocGenerator, which is not in
    # RequiredModules.psd1, and it publishes output/WikiContent, which no task in this build
    # generates. Re-enable it alongside a Generate_Wiki_Content task - see issue #37.
    #- Publish_GitHub_Wiki_Content

####################################################
#       PESTER  Configuration                      #
####################################################

Pester:
  OutputFormat: NUnitXML
  Script:
    # Only run on unit test on './build.ps1 -Task test'
    - ./tests.ps1
  ExcludeTag:
  Tag:

ModuleBuildTasks:
  Sampler:
    - '*.build.Sampler.ib.tasks'

TaskHeader: |
  param($Path)
  ""
  "=" * 79
  Write-Build Cyan "`t`t`t$($Task.Name.replace("_"," ").ToUpper())"
  Write-Build DarkGray  "$(Get-BuildSynopsis $Task)"
  "-" * 79
  Write-Build DarkGray "  $Path"
  Write-Build DarkGray "  $($Task.InvocationInfo.ScriptName):$($Task.InvocationInfo.ScriptLineNumber)"
  ""

GitHubConfig:
  GitHubFilesToAdd:
    - 'CHANGELOG.md'
  GitHubConfigUserName: dscbot
  GitHubConfigUserEmail: dsccommunity@outlook.com
  UpdateChangelogOnPrerelease: false

  Generate_Conceptual_Help:
    MarkdownCodeRegularExpression:
      - '\`(.+?)\`' # Match inline code-block
      - '\\(\\)' # Match escaped backslash
      - '\[[^\[]+\]\((.+?)\)' # Match markdown URL
      - '_(.+?)_' # Match Italic (underscore)
      - '\*\*(.+?)\*\*' # Match bold
      - '\*(.+?)\*' # Match Italic (asterisk)
